/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}",
  ],
  theme: {
    // Strictly structural extensions if needed. 
    // NO colors, typography, or shadows here per project rules.
    extend: {},
  },
  plugins: [],
}